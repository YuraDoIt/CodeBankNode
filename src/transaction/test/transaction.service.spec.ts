import { TransactionService } from '../transaction.service';

describe('Transaction service', () => {
  let transactionService: TransactionService;

  it('Should be defined', async () => {
    // expect(transactionService.createTransaction).toBeDefined();
    // expect(transactionService.delteTransact).toBeDefined();
    // expect(transactionService.getTransaction).toBeDefined();
    // expect(transactionService.clearTransactions).toBeDefined();
  });
});
